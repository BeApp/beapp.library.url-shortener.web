parameters:
    # for "merge" command
    data_to_append:
        require-dev:
            phpunit/phpunit: '^7.3'
            symplify/monorepo-builder: '^4.7'

    # for "split" command
    directories_to_repositories:
        packages/Core: 'git@github.com:BeApp/beapp.library.url-shortener-core.web.git'
